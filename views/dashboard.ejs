<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo Dashboard</title>

    <style>
      @import url("https://fonts.googleapis.com/css2?family=Prosto+One&display=swap");
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }
      body {
        background-image: url("https://cdn1.vectorstock.com/i/1000x1000/13/40/todo-list-seamless-pattern-universal-background-vector-7561340.jpg");
        background-repeat: no-repeat;
        background-position: center;
        background-size: cover;
        font-family: "Prosto One", cursive;
      }
      .heading-logout {
        display: flex;
        align-items: center;
        padding: 1rem;
        box-shadow: 0 2px 4px 0 rgba(0, 0, 0, 0.2);
      }
      img {
        height: 30px;
        width: 30px;
        margin-top: 0.8rem;
      }
      .heading {
        display: flex;
        flex-direction: row;
        /* align-items: flex-start;
        justify-content: flex-start; */
        gap: 1rem;
        width: 50%;
        font-size: 1.4rem;
      }
      h1 {
        text-align: center;
      }
      button {
        width: 20%;
        background-color: rgba(79, 26, 129, 0.815);
        color: white;
        border-radius: 10px;
        padding: 0.8rem;
        border: none;
        cursor: pointer;
        font-family: "Prosto One", cursive;
        transition: 0.3s ease-in-out;
      }
      button:hover {
        background-color: rgba(3, 204, 3, 0.817);
        color: rgb(44, 28, 28);
      }
      .Logout-div {
        width: 50%;
        margin-right: 1rem;
      }
      .logout {
        width: 14%;
        float: right;
      }
      .logout:hover,
      .delete-me:hover {
        background-color: rgba(160, 42, 42, 0.858);
        color: rgb(44, 28, 28);
      }
      input {
        width: 50%;
        height: 5.2vh;
        font-size: 1.2rem;
        padding: 1.3rem;
        margin-top: 0.8rem;
        margin-bottom: 1rem;
        border: none;
        border: 1.8px solid black;
        border-radius: 5px;
        background: transparent;
      }
      input:focus {
        outline: none;
      }
      .form-div {
        margin: 2rem;
        margin-top: 5rem;
      }
      .form-flex {
        display: flex;
        align-items: center;
        gap: 1rem;
      }
      .show-more {
        padding: 1rem;
      }
      .show-more:hover {
        cursor: pointer;
      }
      .item-list {
        background-color: rgba(0, 0, 0, 0.37);
        margin: 2rem;
        padding: 1.9rem;
        list-style: upper-roman;
        border-radius: 10px;
        color: black;
      }
      .item-list li {
        padding: 1rem;
        margin-bottom: 1rem;
      }
      .edit-delete-button{
        width: 100%;
        margin-right: 2rem;
      }
      /* d-flex align-items-center */
    </style>
  </head>
  <body>
    <div class="container">
      <div class="heading-logout">
        <div class="heading">
          <img src="https://cdn-icons-png.flaticon.com/512/1/1560.png" />
          <h1>Todo Dashboard</h1>
        </div>
        <div class="Logout-div">
          <form action="/logout" method="post">
            <button class="logout">Logout</button>
          </form>
        </div>
      </div>

      <div class="form-div">
        <form id="create_form">
          <div class="form-flex">
            <input
              id="create_field"
              name="todo"
              autofocus
              autocomplete="off"
              type="text"
              placeholder="Write your todo"
              style="flex: 1"
            />
            <button class="add_item">Add New Item</button>
          </div>
        </form>
      </div>

      <ul id="item_list" class="item-list"></ul>

      <a id="show_more" class="show-more"> Show More </a>
    </div>
    <script></script>

    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script src="/browser.js"></script>
  </body>
</html>
